#!/bin/bash

echo "Removing Task's directory..."
eval=$(rm -rf Task/)
echo "Copying Task's directory..."
eval=$(cp -r ../RosettaCodeData/Task/ ./)
echo "Initiallizing dataset"
eval=$(./script.fromUpperToLowerCaseAll)
echo "Done removing and coping"
exit
